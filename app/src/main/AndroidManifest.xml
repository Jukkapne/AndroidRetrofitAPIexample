<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required permission for internet access -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Allows the app to access information about networks -->
    <!-- <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> -->

    <!-- Allows the app to write to external storage (e.g., saving images) -->
    <!-- <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" /> -->

    <!-- Allows the app to read from external storage -->
    <!-- <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" /> -->

    <!-- Allows the app to access precise location (GPS and network-based) -->
    <!-- <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" /> -->

    <!-- Allows the app to access approximate location (network-based) -->
    <!-- <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" /> -->

    <!-- Allows the app to use the camera -->
    <!-- <uses-permission android:name="android.permission.CAMERA" /> -->

    <!-- Allows the app to record audio -->
    <!-- <uses-permission android:name="android.permission.RECORD_AUDIO" /> -->

    <!-- Allows the app to access information about Wi-Fi networks -->
    <!-- <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" /> -->

    <!-- Allows the app to read the user's contacts data -->
    <!-- <uses-permission android:name="android.permission.READ_CONTACTS" /> -->

    <!-- Allows the app to send SMS messages -->
    <!-- <uses-permission android:name="android.permission.SEND_SMS" /> -->

    <!-- Allows the app to read SMS messages -->
    <!-- <uses-permission android:name="android.permission.READ_SMS" /> -->

    <!-- Allows the app to receive push notifications via FCM -->
    <!-- <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" /> -->

    <!-- Allows the app to vibrate the device -->
    <!-- <uses-permission android:name="android.permission.VIBRATE" /> -->

    <!-- Allows the app to access the phone's state, such as the phone number and network information -->
    <!-- <uses-permission android:name="android.permission.READ_PHONE_STATE" /> -->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyAPIexampleApp"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyAPIexampleApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>